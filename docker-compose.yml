# Локальный просмотр: docker compose up --build → http://localhost:9080
# На VPS: контейнер на 9080, nginx на хосте слушает 80 и проксирует на 127.0.0.1:9080.
services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "127.0.0.1:9080:80"
