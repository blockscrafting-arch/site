---
/**
 * Превью кейсов на главной: bento-сетка, первый кейс wide.
 * Метрика крупным шрифтом как главный визуал карточки. Без SVG-иллюстраций.
 * CSS reveal, без GSAP.
 */
import { getCollection } from 'astro:content';
const allCases = await getCollection('cases');
const cases = allCases.filter((c) => !c.data.draft).sort((a, b) => (b.data.date?.getTime() ?? 0) - (a.data.date?.getTime() ?? 0));
const featured = cases.slice(0, 4);
---

<section class="py-24 sm:py-32 px-4 sm:px-6 relative overflow-hidden" aria-labelledby="cases-preview-heading">
  <div class="mx-auto max-w-6xl">
    <div class="relative flex justify-center mb-4">
      <div class="relative inline-flex items-center justify-center">
        <!-- SVG-водяной знак строго по центру за текстом -->
        <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 w-64 h-64 sm:w-96 sm:h-96 opacity-[0.15] text-[var(--color-accent)] pointer-events-none -z-10" aria-hidden="true">
          <svg class="w-full h-full animate-glow-pulse" viewBox="0 0 200 200" fill="none" stroke="currentColor">
            <!-- Coordinate system -->
            <path d="M30 170 H170 M30 170 V30" stroke-width="1.5" opacity="0.3" stroke-linecap="round" />
            
            <!-- Faded area under curve (using horizontal lines as a tech pattern) -->
            <path d="M30 140 H60 M30 150 H80 M30 160 H110" stroke-width="1" opacity="0.2" stroke-linecap="round" />
            
            <!-- Main Growth Curve -->
            <path d="M30 140 C 70 140, 110 80, 160 30" stroke-width="3" stroke-linecap="round" />
            <circle cx="160" cy="30" r="6" fill="currentColor" />
            
            <!-- Data Points -->
            <circle cx="85" cy="115" r="4" fill="var(--color-bg)" stroke-width="2" />
            <circle cx="125" cy="72" r="4" fill="var(--color-bg)" stroke-width="2" />
          </svg>
        </div>
        <h2 id="cases-preview-heading" class="reveal font-display text-2xl font-bold text-[var(--color-text)] sm:text-3xl md:text-4xl text-center relative z-10">
          Результаты в деле
        </h2>
      </div>
    </div>
    <p class="reveal mt-4 text-center text-[var(--color-text-muted)] max-w-2xl mx-auto" style="animation-delay:.06s">
      Реальные проекты: боты, агенты, автоматизация.
    </p>

    <div class="reveal-stagger mt-16 grid gap-4 sm:grid-cols-2">
      {featured.map((c, i) => {
        const metric = c.data.metrics?.[0];
        const isWide = i === 0;
        return (
          <a
            href={`/cases/${c.slug}`}
            class:list={[
              'case-preview-card group block rounded-2xl border border-[var(--color-border)] bg-[var(--color-surface)] p-6 sm:p-8 relative overflow-hidden',
              isWide && 'sm:col-span-2',
            ]}
          >
            {/* Верхняя часть: крупная метрика */}
            {metric && (
              <span class:list={[
                'block font-display font-black text-[var(--color-accent)] leading-none',
                isWide ? 'text-5xl sm:text-6xl' : 'text-4xl sm:text-5xl',
              ]}>
                {metric.value}
              </span>
            )}
            {metric && (
              <span class="block mt-2 text-sm text-[var(--color-text-muted)]">{metric.label}</span>
            )}

            <h3 class="mt-6 font-display font-semibold text-[var(--color-text)] text-lg">{c.data.title}</h3>
            <p class="mt-2 text-sm text-[var(--color-text-muted)] line-clamp-2">{c.data.description}</p>

            {/* Glow при hover */}
            <div class="absolute -bottom-24 -right-24 w-48 h-48 rounded-full bg-[var(--color-accent)]/0 group-hover:bg-[var(--color-accent)]/5 blur-3xl transition-all duration-500 pointer-events-none" aria-hidden="true"></div>
          </a>
        );
      })}

      {/* Карточка «Все кейсы» — заполняет пустую ячейку в bento-сетке */}
      <a
        href="/cases"
        class="case-preview-card group flex flex-col items-center justify-center text-center rounded-2xl border border-[var(--color-border)] bg-[var(--color-surface-muted)]/20 hover:bg-[var(--color-surface-muted)]/50 p-6 sm:p-8 relative overflow-hidden transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-[var(--color-accent)] focus-visible:ring-offset-2 focus-visible:ring-offset-[var(--color-bg)]"
      >
        <span class="text-xl font-display font-medium text-[var(--color-text)] group-hover:text-[var(--color-accent)] transition-colors">
          Все кейсы <span class="inline-block transition-transform duration-300 group-hover:translate-x-1">→</span>
        </span>
        <div class="absolute -bottom-24 -right-24 w-48 h-48 rounded-full bg-[var(--color-accent)]/0 group-hover:bg-[var(--color-accent)]/5 blur-3xl transition-all duration-500 pointer-events-none" aria-hidden="true"></div>
      </a>
    </div>
  </div>
</section>
