# Локальный просмотр: docker compose up --build → http://localhost:8080
# На VPS: контейнер на 8080, nginx на хосте слушает 80 и проксирует на 127.0.0.1:8080.
services:
  web:
    build:
      context: .
      dockerfile: docker/Dockerfile
    ports:
      - "127.0.0.1:8080:80"
